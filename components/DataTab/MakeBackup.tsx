import React, { FC } from 'react';
import { Text } from 'react-native';
import { MakeBackupProps } from '../../types';
const MakeBackup: FC<MakeBackupProps> = () => {
	return <Text>Backup stuff</Text>;
};
export default MakeBackup;
