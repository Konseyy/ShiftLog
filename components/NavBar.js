import react from "react";
import { Text } from "react-native";
const FooterNav = ({ state, descriptors, navigation }) =>{
    return (
        <Text>
            Hello
        </Text>
    )
}
export default FooterNav;